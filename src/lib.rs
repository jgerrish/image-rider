/// The disk_format module contains everything to parse disk formats
#[warn(missing_docs)]
#[warn(unsafe_code)]
pub mod disk_format;

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        let result = 2 + 2;
        assert_eq!(result, 4);
    }
}
